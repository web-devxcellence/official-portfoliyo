import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

// Updated metadata with favicon and title
export const metadata = {
  title: "Yash Singh – Portfolio",
  description: "Full Stack Developer | Designer | Creative Coder",
  icons: {
    icon: ["/favicon.svg", "/favicon.ico"], // SVG first (modern), ICO fallback
    apple: "/favicon.ico",
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
